# Capability Adoption Lifecycle

`discovered -> quarantined -> schema-validated -> provenance-verified -> policy-evaluated -> sandbox-tested -> locally-active -> task-proven -> monitored`

Each destination may reject at any stage. Activation is atomic, retains the prior version, and produces a rollback receipt. Network popularity and CodeRabbit review are evidence, not local authority.
